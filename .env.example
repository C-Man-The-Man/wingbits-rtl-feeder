# --------------------------------------------------------
# Wingbits RTL Feeder - Environment Variables
# --------------------------------------------------------
# Copy this file to .env and update your values
# All variables are optional unless marked REQUIRED
# --------------------------------------------------------

# -------------------------------
# Wingbits feeder configuration
# -------------------------------

# REQUIRED: Unique station / device ID
# Can be:
# - Animal name format: your-animal-name
# - Device serial: 18-character hex
# - UUID format: xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
WINGBITS_DEVICE_ID=

# REQUIRED: Feeder location
FEEDER_LAT=     # Latitude in decimal degrees, check your Wingbits dashboard
FEEDER_LONG=    # Longitude in decimal degrees, check your Wingbits dashboard
FEEDER_ALT_M=   # Altitude in meters
FEEDER_TZ=      # Timezone, e.g., Europe/Madrid
FEEDER_NAME=    # Friendly name for TAR1090 title

# -------------------------------
# RTL-SDR / receiver configuration
# -------------------------------

# OPTIONAL: RTL-SDR serial number to deterministically select the correct device
# Use `rtl_eeprom` or `rtl_test -t` to find it
# Useful when more than one RTL-SDR devices are connected to the host machine
# Leave blank for device 0 automatic selection
# Example: 00000001
ADSB_SDR_SERIAL=

# OPTIONAL: PPM correction for the RTL-SDR (leave blank for default)
ADSB_SDR_PPM=

# OPTIONAL: Receiver gain setting (default: autogain)
# Leave commented for autogain, or set a specific value (e.g. 49.6)
# Common values: -10 (autogain), 0-49.6 (manual gain in dB)
READSB_GAIN=-10

#--------------------------------
# Wingbits Geosigner
#--------------------------------

# REQUIRED: The default path, change if necessary, use ls /dev/tty* to verify
SERIAL_DEVICE=/dev/ttyACM0

# -------------------------------
# Notes
# -------------------------------
# 1. All coordinates, altitudes, and serial numbers must be set accurately.
# 2. Gain can be set to -10 for autogain or a specific dB value.
# 3. After editing, save as ".env" and run "docker compose up -d"

